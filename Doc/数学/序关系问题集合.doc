问题一：
用关系'<'和'='连接A,B,C三个数，有13种不同的排列关系:
	A<B<C,A<B=C,A<C<B,A=B<C,A=B=C A=C<B;
	B<A<C,B<A=C,B<C<A,B=C<A;
	C<A,B,C<A=B,C<B<A;
编程求出N个数依序排列时有多少种关系.
	下面用动态规划的方法解决这个问题，设F[n]表示有N个数的总排列数;那么状态转移为：
	F[N] = C[1,N]*F[N-1]+C[2,N]*F[N-2]+C[3,N]*F[N-3]+......C[N,N]*F[N-N];（F[0]=1）
	意思是枚举最后一个不等号出现的位置。
小结，这是动规的思想，其实这个就是一个等价划分，再排列问题。

问题二：
对于一个固定的n元排列，它的逆序数存在且唯一。那么反过来，已知一个n元排列的逆序数为m，这样的n元排列有多少种？
	公式：f(n+1,m)=f(n,m)+f(n,m-1)+...+f(n,m-n)(注:记b<0时，f(a,b)=0)
	详细解释可以看逆序数知识。

问题三：
	对于可重复取的集合A={a,b,c...},取出不同的K组合数的个数？
	公式ans = C(n + k – 1, k).
　　
